<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container">
    	<a class="navbar-brand" th:href="@{/}">Home</a>
        <div class="collapse navbar-collapse" id="navbarColor02">
        	<ul class="navbar-nav mr-auto nav">
	            <li class="nav-item dropdown">
	            	<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" aria-haspopup="true" aria-expanded="false">Categories</a>
	            	<div class="dropdown-menu">
				    	<a class="dropdown-item" th:each="category: ${categories}" th:text="${category.name}"  th:href="@{/books{id}(id=${category.getId()})}"></a>
				    </div>
	            </li>
	            
	            <li class="nav-item dropdown">
	            	<a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" aria-haspopup="true" aria-expanded="false">Stores</a>
	            	<div class="dropdown-menu">
				    	<a class="dropdown-item" th:each="store: ${stores}" th:text="${store.name}"  th:href="@{/storeBooks{id}(id=${store.getID()})}"></a>
				    </div>
	            </li>
            
	            <li  th:if="${user != null} AND ${user.admin != 1}" class="nav-item">
	            	<a class="nav-link " th:href="@{cart}">Cart</a>
	            </li>
            	
            	<li   th:if="${user != null} AND ${user.admin == 1}" class="nav-item">
              		<a class="nav-link" th:href="@{admin}">Dashboard</a>
            	</li>
          	</ul>
 
          	<form class="form-inline my-2 my-lg-0" method="post" th:action="@{/search}">
	            <input  class="form-control mr-sm-2" type="text" name="pat" placeholder="Search"/>
            	<button class="btn btn-secondary my-2 my-sm-0 fa fa-search" type="submit"></button>
          	</form>
          	
	      	<ul class="navbar-nav">
	        	<li class="nav-item my-2 my-lg-0">
	          		<a class="nav-link" th:if="${user == null}" th:href="@{login}">Login</a>
	          		<a class="nav-link" th:unless="${user == null}" th:href="@{logout}">Logout</a>
	         	</li>
	      	</ul>
        </div>
	</div>
</nav>
